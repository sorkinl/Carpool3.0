// import { authReducer } from "../redux/reducers/authReducer"; //is this okay?

export default function authHeader() {
    // const user = authReducer.user;
    // const userToken = authReducer.token;
    // // JSON.parse(localStorage.getItem('user'));
    // console.log("auth-header: user = ", user);
    // console.log("auth-header: userToken = ", userToken);

    // if (user && userToken) {
    //   // for Node.js Express back-end
    //   return { 'x-access-token': userToken };
    // } else {
    //   return {};
    // }
  }